/**
 * 原稿（文章だけをまとめた1枚）を posts.js から組み立てる。
 *
 * 分担が「デザイン＝ChatGPT／構成と文章＝こちら」に分かれたので、
 * 文章だけを1ファイルにまとめて渡せるようにしている。
 *
 * 手で書き写さないのが要点。posts.js を直して流せば、原稿も必ず一緒に変わる。
 * 二重管理にすると必ずどちらかが古くなる。
 *
 *   npm run ig:copy       docs/instagram-9-原稿.md を書き出す
 *   npm run ig:handoff    引っ越しフォルダ（handoff/）ごと組み立てる
 *
 * buildCopyDoc() は build-handoff.mjs からも呼ばれる。
 */

import { writeFileSync } from 'node:fs'
import { fileURLToPath } from 'node:url'
import { dirname, resolve } from 'node:path'

import { posts, hashtagsFor, PROFILE_FOOTER, SERIES_TAGLINE } from '../src/instagram9/posts.js'

const root = resolve(dirname(fileURLToPath(import.meta.url)), '..')
const out = resolve(root, 'docs/instagram-9-原稿.md')

/** 表紙に置く文字。デザインを作る側が拾うのはここだけ。 */
const coverBlock = p => {
  const sub = p.sub ? (Array.isArray(p.sub) ? p.sub : [p.sub]) : []
  return [
    `| カテゴリー | \`${p.category}\` |`,
    `| 見出し | ${p.headline.map(l => `\`${l}\``).join(' ／ ')} |`,
    sub.length
      ? `| サブ | ${sub.map(l => `\`${l}\``).join(' ／ ')} |`
      : '| サブ | なし |',
  ].join('\n')
}

const section = p => `## ${p.no}｜${p.category}

### 表紙に入れる文字

| | |
| --- | --- |
${coverBlock(p)}

**この回の役割**　${p.role}

**写真**　${p.photoNote}

### 本文

\`\`\`
${p.caption}

${hashtagsFor(p.no)}
\`\`\`

本文 ${p.caption.length}字（プロフィール定型を含む）
`

export const buildCopyDoc = () => `# Instagram 9投稿｜原稿

**このファイルは文章だけを扱います。デザインは \`docs/instagram-9-引っ越しセット.md\` です。**

| 中身 | 担当 | ファイル |
| --- | --- | --- |
| 全体構成・表紙の文字・本文・ハッシュタグ | Claude | このファイル（元は \`src/instagram9/posts.js\`） |
| 表紙のデザイン・写真の選定と配置 | ChatGPT | \`docs/instagram-9-引っ越しセット.md\` |

**手で書き換えないでください。** 元は \`src/instagram9/posts.js\` で、
\`npm run ig:copy\` を流すとこのファイルが作り直されます。

---

## 判断の基準

> **${SERIES_TAGLINE}**

コピーを足すときも直すときも、必ずこの一文に接続できるかを先に確認します。

01〜09は独立した9投稿ではなく、一本の物語です。

\`\`\`
人生 → 違和感 → 選択 → 働き方 → 仕組み → AI → 人 → 世界 → 経営者支援 → 人生の選択肢
\`\`\`

### 本文の書き方（投稿済みの01・02から取った）

実際に投稿された2本を読み直して決めました。**ここから外れると本人の文章に見えません。**

1. **箇条書き（・）を使わない。** 01にも02にも1つもありません。全部が地の文です。
   箇条書きが入ると、体験談ではなく解説記事に見えます
2. **1行は23字まで。** 投稿済みの最長が01で23字、02で22字。スマホで折り返さない幅です。
   長い一文は意味の切れ目で改行して2行にします
3. **1段落は1〜3行。** 段落の間は必ず1行あけます
4. **抽象語ではなく、具体的な事実。**「4年間の産休・育休」「小学生2人の子育てと、全国への出張」
   「15年以上、抱えていました」。数字と場面があります
5. **心が動いたところは、そのまま書く。**「最終日は、涙が止まりませんでした。」
   説明せず、起きたことだけを置きます
6. **最後は短い一文で言い切る**
7. **敬体と常体は回ごとにそろえる**（01は常体、02は敬体。1本の中で混ぜない）
8. **ハッシュタグは1行ずつ縦に並べる**
9. **「会社を辞めたから」を理由にしない。** 辞めたことを原因として書くと、
   会社員のまま働き方を変えたい人と副業の人が全員はじき出されます。
   変わったのは所属ではなく、**時間の扱い方と頼り方**。そちらを原因に書きます
10. **示せるなら説明しない。** 01・02は結論を説明せず、起きたことだけを置いています
11. **できなかった → できた、で切り返さない。**「でも、行けました」のような書き方は、
    読む人を「できた人／まだの人」に分けます。自慢の形です。
    **悩んでいた側にとどまる時間を長く取る。** そこが読む人と同じ場所です。
    できたことは、後半に一度だけ、順番を変えたことの副産物として置きます

### 中身を決めるときの決まりごと

- **読む人を「会社を辞めた人」だけにしない。** 会社員のまま働き方を変えたい人、
  副業を始めた人、家のことを抱えている人が「自分の話ではない」と離れないようにする
  （08だけは経営者支援の回なので例外）
- **並べるものは、読む人が持っているものから。** 時間の使い方・優先順位・
  夫婦の時間・子どもとの過ごし方。お客様や売上から始めない
- **戦略の話は持ち主の回まで取っておく。**
  手放す・任せる・仕組みにする＝04／頼ることの怖さ・チームづくり＝06／
  関わる人の選択肢まで広げる＝09
- **1つの記事を1つの投稿で使い切らない。** 一番強い一行はその回のために残す
- ハッシュタグは**1投稿5つまで**。1枠目はシリーズのタグで固定

### 本文の末尾に置く定型ブロック

\`\`\`
${PROFILE_FOOTER}
\`\`\`

「01〜09で、ひとつの物語になっています」は入れません。連載であることは
本文の頭の「01｜」で足りていて、説明を足すと着地のあとに案内文が来て弱くなります。

---

${posts.map(section).join('\n---\n\n')}`

// 直接叩かれたときだけ docs/ へ書き出す（import されたときは何もしない）
if (process.argv[1] && resolve(process.argv[1]) === fileURLToPath(import.meta.url)) {
  writeFileSync(out, buildCopyDoc())
  console.log(`docs/instagram-9-原稿.md を書き出しました（${posts.length}投稿）`)
}
